name: Manual Release (Emergency/Fallback)

# This workflow is a FALLBACK for emergency situations.
# For normal releases, use: "Release Start" → "Release Finish" workflows
# Only use this if the gitflow workflows fail or for emergency releases.

on:
  workflow_dispatch:
    inputs:
      git_ref:
        description: 'Git tag to release (e.g., v2.0.16). Tag must already exist!'
        required: true
        type: string

jobs:
  publish-release:
    name: Publish release to Maven Central
    uses: entur/ror-gha-workflows/.github/workflows/release-manual.yml@v1
    with:
      git_ref: ${{ inputs.git_ref }}
      java_version: 11
      java_distribution: liberica
      version_tag_prefix: v
    secrets: inherit